"""CV-Pilot shared library package.

Houses domain models, database access, and error types used by the
`skills/database/scripts/query.py` CLI. Imported as a plain package from the
repository root (`cv-pilot-agent/`), which is added to ``sys.path`` by the CLI
entrypoint.
"""